# Colombia Contrata — Contexto del proyecto

> Este archivo es la memoria portátil del proyecto: vive dentro del repo (a diferencia de la memoria propia del asistente, que queda atada a esta máquina), así que viaja con `git clone` a cualquier otra computadora o sesión. Mantenlo actualizado en vez de depender solo de memoria externa.

## Qué es
Portal web para obtener documentos requeridos en contratación pública en Colombia (contraloría, policía, procuraduría, antecedentes penales, registros de conductas sexuales, entre otros).

Dos perfiles de usuario:
- **Independientes/personas naturales**: se registran, pagan, seleccionan checklist de documentos. La plataforma consulta vía API a un proveedor de fuentes, genera los PDFs, notifica por email, y deja el comprimido disponible para descarga por 10 días.
- **Empresas**: compran paquetes/planes de consultas (créditos) para validar antecedentes de candidatos antes de contratarlos. El candidato debe autorizar él mismo (Habeas Data) — la empresa invita, el candidato entra a la plataforma y autoriza; no se puede consultar a un tercero sin su propio consentimiento.

Ver `README.md` en la raíz del proyecto para el detalle técnico completo (stack, estructura, páginas implementadas, roadmap). Este archivo se enfoca en contexto de colaboración y decisiones que no están en el README.

## Estado actual (resumen)
Sitio en producción: https://colombiacontrata.com (dominio propio ya conectado; también responde en https://colombia-contrata.vercel.app) — cada push a `main` despliega automático en Vercel.

Páginas construidas: `/` (landing — **ahora es un server component async** que lee `configuracion_landing` con `export const revalidate = 60` para los textos del encabezado/"Cómo funciona"/títulos de sección y sus interruptores mostrar-ocultar; "Planes para empresas" lee `planes_empresa` en vivo con toggle mensual/anual, "Documentos disponibles" lee `precios_documentos` sin precio, la tarjeta "Persona independiente" lee `configuracion_persona`), `/registro` y `/login` (**ambos conectados a Supabase Auth real** — signUp/signInWithPassword), `/perfil` (**conectado de verdad** — lee y guarda en la tabla `profiles` de Supabase según persona/empresa, más una sección "Seguridad de la cuenta" con `AccountSecurityForm.tsx` para cambiar contraseña/correo vía `supabase.auth.updateUser`), `/historial` (placeholder protegido por sesión, "Aún no tienes solicitudes" — falta el flujo real de documentos), `/terminos`, `/privacidad` (borradores legales), `/paginas/[slug]` (ruta dinámica, páginas propias creadas desde `/admin` → Páginas, ej. `/paginas/nosotros` — 404 real si no existe/inactiva), `/admin` (**protegido con autenticación real** vía `AdminGate.tsx`, solo cuentas con `app_metadata.role = "admin"`; desde 2026-08-08 es un **menú lateral agrupado** en vez de pestañas horizontales — Identidad del portal / grupo "Página principal" (Textos y secciones, Bloques de contenido, Páginas) / grupo "Planes y documentos" (Planes de personas, Planes de empresa, Documentos disponibles) / Administradores — todas guardan de verdad).

**Pestaña "Página principal"** (2026-08-08, a pedido del usuario "me gustaría poder editar agregar o quitar secciones y textos de la página principal desde admin"): se le preguntó qué tan flexible lo quería (texto+mostrar/ocultar vs. bloques genéricos vs. constructor de páginas completo) y eligió la opción simple — **no** se puede agregar una sección con diseño propio nuevo, solo editar los textos de las secciones que ya existen (encabezado, "Cómo funciona" con sus 3 pasos, títulos de "Documentos disponibles" y "Planes") y prender/apagar cada una (`configuracion_landing`, `LandingConfigManager.tsx`). El encabezado principal (hero) no tiene interruptor, siempre se muestra.

**Pestaña "Bloques de contenido"** (2026-08-08, la escalada natural mencionada arriba — el usuario preguntó "y si quisiera agregar más bloques y contenido (texto imágenes botones etc.)" y luego pidió construirlo): tabla `bloques_landing` (multi-fila, a diferencia de las demás tablas de config que son fila única). Cada bloque tiene título/descripción opcionales, imagen opcional (sube a Storage `portal-assets`, con posición izquierda/derecha), botón opcional (label+href), `activo`, y `orden` (se reordena con flechas ▲▼ en `BloquesLandingManager.tsx`, que intercambian el campo `orden` entre bloques vecinos — no es drag-and-drop). Se renderizan al final de `/` antes del Footer, alternando fondo claro/gris automáticamente por posición. Verificado end-to-end creando y luego borrando un bloque de prueba.

**Rediseño del panel admin a menú lateral** (2026-08-08, mismo día): al llegar a 7 secciones, el usuario pidió agrandar el contenedor (otra vez — ya se había hecho una vez antes, de `max-w-4xl` a `max-w-6xl`) y además **agrupar por categoría** (dio el ejemplo explícito de agrupar Planes de personas + Planes de empresa + Documentos disponibles). En vez de seguir agrandando pestañas horizontales indefinidamente, se cambió `AdminTabs.tsx` a un **menú lateral** (`sm:w-60`, sticky, con grupos de encabezado no clicable + sub-ítems) y el contenedor a `max-w-7xl`. Si se agregan más secciones en el futuro, es la estructura correcta para seguir creciendo (agregar al array `nav`, dentro de un grupo existente o uno nuevo) — no volver a pestañas horizontales.

⚠️ **Gotcha de esta máquina — `window.confirm()` no se puede aceptar en el navegador automatizado**: el botón "Eliminar" de `BloquesLandingManager.tsx` y `PlanesEmpresaManager.tsx` usa `confirm(...)` antes de borrar; en el navegador real del usuario funciona normal, pero en el navegador automatizado de Claude Code el diálogo nativo no se puede confirmar (el clic no tiene efecto). Para borrar filas de prueba durante una verificación, hacerlo por SQL directo en Supabase en vez de por la UI.

`Header.tsx` es client component: con sesión activa muestra un menú desplegable (círculo con iniciales, nombre en azul negrilla leído de `profiles`, y dentro del menú nombre+correo arriba y accesos con ícono a Inicio/Historial/Perfil/Cerrar sesión). Para personas el nombre mostrado es `primer_nombre` + `primer_apellido` (no solo el primer nombre — corregido 2026-08-08 a pedido del usuario), con el correo como respaldo si el perfil aún no existe; para empresas sigue siendo `razon_social`. Las iniciales del círculo: para personas es primera letra de `primer_nombre` + primera letra de `primer_apellido`; para empresas las iniciales de las dos primeras palabras de `razon_social`. También trae el `ThemeToggle` (modo claro/oscuro, ver más abajo), visible con o sin sesión.

⚠️ **Gotcha de enlaces de ancla fuera de `/`** (2026-08-08, mismo día, reportado por el usuario probando `/paginas/nosotros`): `navLinks` en `Header.tsx` (Cómo funciona/Documentos/Planes/Empresas, se repite en la barra de escritorio Y en el menú móvil) y la columna "Plataforma" de `Footer.tsx` usaban `href="#planes"` sin `/` — como Header y Footer están en todas las páginas, no solo en `/`, ese enlace en `/paginas/nosotros` no llevaba a ningún lado (solo cambia el fragmento de la URL actual, y ahí no existe ningún `id="planes"`). Se corrigió a `href="/#planes"` (con `/` adelante) en los tres lugares donde aparece (nav de escritorio, menú móvil, footer). El botón "Soy empresa" del hero en `page.tsx` no necesitó el cambio porque solo existe dentro de `/`. Si se agrega un enlace de ancla nuevo en un componente que se muestre en más de una página, siempre escribirlo como `/#seccion`, nunca `#seccion` a secas.

**Redes sociales y botón de WhatsApp** (2026-08-08, a pedido del usuario "quiero agregar una sección en el pie de página para redes sociales y un botón flotante de whatsapp con número y mensaje configurable"): se extendió `configuracion_portal` (misma tabla singleton de identidad del portal, editable en la misma pestaña de `/admin`) con `facebook_url`/`instagram_url`/`twitter_url`/`linkedin_url`/`tiktok_url` y `whatsapp_numero`/`whatsapp_mensaje`/`whatsapp_activo`. `Footer.tsx` pasó de estático a **server component async** y muestra un ícono por red social que tenga URL configurada (ninguna es obligatoria). `WhatsAppButton.tsx` (server component async, nuevo) se agregó en `layout.tsx` — aparece en **todo el sitio**, fijo abajo a la derecha, y solo se renderiza si `whatsapp_activo=true` y hay número guardado. Íconos SVG en `SocialIcons.tsx`. Verificado end-to-end con datos de prueba (número y URLs falsos) y luego limpiado.

⚠️ **Gotcha de esta máquina — los inputs de texto no responden a borrar con teclado (`triple_click`+`Delete`, `Ctrl+A`+`Backspace`)**: al intentar limpiar campos de prueba (URLs de redes, número de WhatsApp) en `AdminSettingsForm.tsx`, seleccionar y borrar con el teclado no cambió el valor real del input (confirmado leyendo `input.value` por JS — seguía con el valor viejo). Es la misma familia de problema que el `window.confirm()` no confirmable (ver abajo) — el navegador automatizado de esta máquina no siempre logra que las interacciones de teclado/diálogo lleguen al estado de React. Solución: actualizar por SQL directo en Supabase en vez de pelear con la UI.

**Bloques de contenido — tamaño real de imagen + color de fondo** (2026-08-08, mismo día): el usuario reportó que las imágenes de los bloques "quedan de un tamaño muy grande y no del tamaño original" — la causa era que `page.tsx` forzaba `w-full sm:w-1/2 object-cover` en el `<img>`. Se corrigió quitando ese forzado (ahora respeta el tamaño natural, con `style={{ maxWidth: "min(Npx, 100%)" }}` donde `N` = nuevo campo `imagen_ancho`, opcional, default 400). Luego pidió también poder configurar el color de fondo por bloque — se agregó `fondo_color` (hex, opcional; si se deja vacío sigue alternando claro/gris automático como antes).

**Páginas propias** (2026-08-08, mismo día — la escalada más grande de la sesión): el usuario pidió poder crear páginas nuevas (ej. "Nosotros": visión, misión, servicios) y enlazarlas desde los botones de los Bloques de contenido. Se le preguntó (a) qué tan flexible el contenido — eligió simple (título + un cuerpo de texto) pero **"editable como un Word o que acepte HTML"**, y (b) si las páginas deben aparecer solas en el menú — eligió manual **con un checkbox opcional** para mostrarlas en el menú si se quiere. Resultado: tabla `paginas` (slug único = URL, titulo, contenido HTML, activo, mostrar_en_menu, orden), ruta pública `src/app/paginas/[slug]/page.tsx` (404 real si no existe/inactiva), pestaña "Páginas" en `/admin` (agrupada junto a Textos y secciones / Bloques de contenido), y `Header.tsx` agrega automáticamente al nav (desktop+móvil) las páginas con `mostrar_en_menu=true`. El "editor tipo Word" (`RichTextEditor.tsx`) se construyó a mano con `contentEditable` + `document.execCommand` (negrita/cursiva/subrayado/H2/H3/listas/enlaces) **sin agregar ninguna librería npm nueva** — decisión deliberada para no arriesgar una instalación lenta/inestable en la unidad de red (ver gotcha de entorno más abajo). Verificado end-to-end: creé una página de prueba, apliqué negrita con el editor, marqué "mostrar en el menú", confirmé que la página cargaba en `/paginas/<slug>` y que el enlace aparecía en el menú móvil; luego la borré.

**Rediseño de la plantilla de páginas propias — probado y revertido** (2026-08-08): a pedido de "un diseño más profesional conservando el contenido" se cambió `src/app/paginas/[slug]/page.tsx` a un encabezado tipo hero (fondo `bg-brand-navy`, título grande blanco, enlace "Volver al inicio") + divisores entre secciones `<h2>` + listas con check (✓). El usuario respondió "no me gusta este diseño" — específicamente el hero navy y los divisores, y "en general prefiero algo más simple". Se revirtió por completo a la plantilla original (`git show <commit-anterior>:archivo > archivo`, más simple que reescribir a mano): solo `<h1>` + contenido con selectores `[&_h2]`/`[&_h3]`/listas normales, sin fondo de color ni separadores. **Lección**: para esta plantilla compartida (afecta "Nosotros" y cualquier página futura), el usuario prefiere minimalismo — no reintentar un rediseño visual llamativo sin preguntar primero qué tan sobrio lo quiere.

**Estilo de "Nosotros" adoptado en la página principal** (2026-08-08, mismo día): al usuario le gustó tanto el diseño de "Nosotros" que pidió aplicarlo también en `/`, "incluso las líneas de colores en la parte superior". A diferencia de "Nosotros" (HTML/CSS estático), aquí el mismo lenguaje visual se **tradujo a Tailwind** en `page.tsx` + `PreciosDocumentosPricing.tsx`/`PlanPersonaCard.tsx`/`PlanesEmpresaPricing.tsx`, para no perder nada dinámico (textos de admin, planes/documentos en vivo) ni el modo oscuro. La franja de colores (amarillo/azul/rojo) se agregó **después** de `<Header />` en `page.tsx`, no dentro — el usuario pidió que "se oculte cuando haga scroll", y como `Header.tsx` es `sticky top-0`, ponerla dentro del Header la habría dejado pegada arriba para siempre; puesta fuera, en el flujo normal, desaparece con el primer scroll mientras el menú se queda fijo. Solo está en `/` por ahora, no en todo el sitio.

**Insignia del hero quitada** (2026-08-08, mismo día): el usuario pidió quitar el texto "Colombia Contrata" que aparecía como una pequeña insignia (punto + texto) encima del título del hero en `/`. Se eliminó el `<span>` completo, no solo el texto — dejarlo vacío habría dejado una píldora sin contenido.

**Imagen Open Graph para compartir en redes sociales** (2026-08-08, mismo día): el usuario pidió usar `og-image.png` (de `\\NAS-YORBIS\personal_folder\SSA\Proyectos SSA\Colombia Contrata\Imagenes\og-image.png`, 1200×630, ya diseñada por él) como vista previa al compartir el enlace del sitio por WhatsApp/redes. Se copió a `public/og-image.png` y se agregó `metadataBase` + bloques `openGraph`/`twitter` (`summary_large_image`) en `generateMetadata()` de `layout.tsx`. Título/descripción/imagen quedaron fijos en código (no leídos de `configuracion_portal`) — si se quiere que sean editables desde `/admin` en el futuro, habría que agregar columnas nuevas ahí, mismo patrón que `favicon_url`. Verificado en producción leyendo los `<meta property="og:...">` del HTML servido.

**Página "Nosotros" con diseño completo personalizado** (2026-08-08, mismo día): el usuario pegó un HTML/CSS completo (hero, tarjetas, secciones con gradientes — un diseño tipo landing page) y pidió aplicarlo a "Nosotros". `src/app/paginas/[slug]/page.tsx` ahora detecta `contenido?.includes("<style")` y, si es cierto, renderiza a **ancho completo** sin el `<h1>` automático ni el `max-w-3xl` (el HTML pegado ya trae su propio encabezado/maquetación); si no hay `<style>`, sigue el comportamiento simple de siempre — así que esto no rompe páginas simples futuras. El contenido (~24.000 caracteres) se cargó directo por SQL en vez de por el textarea del admin, solo por tamaño/velocidad — el modo código HTML del editor también lo habría aceptado. Todas las clases CSS se prefijaron con `.nosotros-page` (con un `<div class="nosotros-page">` envolvente) para que los estilos no se filtren a otras páginas al navegar sin recargar. Se corrigieron 3 `href="#"` de ejemplo a rutas reales. Después el usuario pidió más ancho ("quiero que ocupe más ancho de la página") — se intentó ampliar `.container` de `min(1180px, 92%)` a `min(1440px, 95%)` con un `UPDATE ... SET contenido = replace(contenido, '...', '...')`, pero **al revisar la BD el 2026-08-09 el valor seguía en `min(1180px, 92%)`** — el replace no tomó efecto (causa no diagnosticada) y nadie lo verificó en vivo en su momento. Si se retoma este pedido, reintentar el `UPDATE` y confirmar midiendo `.container` en el navegador, no solo el mensaje de éxito de Supabase.

**Términos y Privacidad rediseñadas y convertidas en páginas editables** (2026-08-09): el usuario pidió primero "como a la página de Nosotros, ponle diseño también" a `/terminos` y `/privacidad`, y en el mismo turno "también convertilo en páginas editables desde admin". Se hizo en dos pasos: (1) mismo lenguaje visual del resto del sitio (hero con degradado e ícono, tarjeta redondeada, secciones numeradas con círculo azul — pero el número ya no se escribe a mano en el texto, sale solo vía **CSS counter** en una clase `.legal-content` nueva en `globals.css`, así el admin solo escribe `<h2>Objeto</h2>` sin número); (2) ambas páginas pasaron a leer `titulo`/`contenido` de la tabla `paginas` (mismo sistema que "Nosotros"), con un HTML/título hardcodeado como respaldo si la fila no existe. Como estas dos páginas tienen ruta fija (`/terminos`, `/privacidad`, no `/paginas/<slug>`), se agregaron tres guardas para que no queden duplicadas: `paginas/[slug]/page.tsx` hace 404 para esos dos slugs, `Header.tsx` los excluye del menú automático, y `PaginasManager.tsx` los detecta (`SLUGS_ESPECIALES`) mostrando la URL real y bloqueando la edición del slug en el formulario. De paso, a pedido explícito del usuario, se quitó el aviso amarillo "Este documento es una plantilla base..." (`LegalDisclaimer.tsx` eliminado por completo, ya no se usaba en ningún otro lado) y se igualó el ancho de ambas al de "Nosotros" (`max-w-[1180px]`, el valor real medido — ver el gotcha del `replace()` arriba).

**Franja de colores de marca ahora en todo el sitio** (2026-08-09, mismo día): el usuario pidió que la franja (que solo vivía en `page.tsx` de la home) apareciera "siempre en todas las páginas", incluidas Términos/Privacidad. Se movió a `Header.tsx`: el componente ahora devuelve un fragment con el `<header>` sticky y la franja como **hermana** después de él (mismo cuidado de siempre — si estuviera dentro del sticky quedaría pegada arriba para siempre). Como `Header.tsx` se usa en todas las páginas, la franja aparece automáticamente en cualquier ruta nueva sin tener que acordarse de agregarla — verificado en `/`, `/terminos`, `/privacidad` y `/login`.

**Modo código HTML en el editor de páginas** (2026-08-08, mismo día, a pedido de "que también se pudiera editar el texto en formato HTML"): se agregó un botón `</>` a `RichTextEditor.tsx` que alterna entre la vista visual y un `<textarea>` con el HTML crudo — pensado sobre todo para limpiar el HTML "sucio" que deja pegar contenido desde Word/Google Docs (`class="MsoNormal"`, estilos `mso-*` inline, etc. — se vio ese caso real al abrir el modo código en la página "Nosotros"). Detalle técnico: el estado `htmlValue` es la única fuente de verdad al cambiar de modo (se lee del DOM solo al momento de cambiar a código, no en cada tecla en modo visual, para no perder la posición del cursor).

**Página "Nosotros" ya publicada con contenido real** (2026-08-08, mismo día): el usuario compartió `\\NAS-YORBIS\personal_folder\SSA\Proyectos SSA\Colombia Contrata\Brand Book\Capitulo_01_ADN_Marca_Colombia_Contrata.docx` (había un `.md` ya exportado al lado, no hizo falta convertir el Word — `pandoc` no está instalado en esta máquina, ver nota). Se creó `/paginas/nosotros` con el contenido oficial: Introducción, Filosofía, Propósito, Misión, Visión, Valores, Personalidad de marca, Promesa de marca — insertado directo por SQL (no a mano por el editor, para no pelear con la automatización de teclado). **Esa misma carpeta de red tiene los capítulos 2 a 4** (logotipo, construcción del logotipo, sistema visual) — revisar si se retoma trabajo de identidad de marca. Nota: `pandoc` no está disponible en esta máquina para convertir `.docx` — cuando no haya un `.md` ya exportado al lado, la alternativa es `unzip` el `.docx` y leer `word/document.xml` directamente (es un ZIP de XML).

**Correo de contacto y texto legal del footer editables** (2026-08-08, mismo día): a pedido del usuario, se agregaron `correo_contacto` (default `contacto@colombiacontrata.com`) y `footer_texto` (default `Todos los derechos reservados.`) a `configuracion_portal`, editables en `/admin` → Identidad del portal. `Footer.tsx` arma el copyright como `© {año automático} {nombre_portal}. {footer_texto}` — es la primera vez que `nombre_portal` se usa de verdad en el front (antes solo se guardaba).

**Tamaños del logo/avatar** (ajustados 2026-08-08 a pedido del usuario, "que se vea un poco más grande"): el isotipo del Header pasó de 32px a 44px (`size-11`/`h-11`). El círculo de iniciales del panel de menú móvil (el que abre el ícono de hamburguesa) pasó de 32px a 40px (`size-10`) para que se vea proporcional; el del menú desplegable de escritorio se dejó en 28px (`size-7`, no se tocó).

**Modo oscuro**: todo el sitio lo soporta. Mecanismo: `globals.css` declara `@custom-variant dark (&:where(.dark, .dark *));` (Tailwind v4 no trae dark-por-clase de fábrica) para que `dark:` responda a una clase `.dark` en `<html>`, no a `prefers-color-scheme`. `layout.tsx` inyecta un script `beforeInteractive` que aplica esa clase antes de hidratar (evita parpadeo; por eso `<html suppressHydrationWarning>`). `ThemeToggle.tsx` alterna la clase y persiste en `localStorage` (`theme: "light"|"dark"`). Si se agrega una página/componente nuevo, hay que acordarse de sumarle variantes `dark:` — no es automático.

Backend: **Supabase confirmado y conectado** (Auth + Postgres + Storage). Proyecto `colombia-contrata`, ref `zjbijmieiyumpqwyqhfm`, región São Paulo. Cliente en `src/lib/supabase.ts`, usa `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`. Tablas: `profiles` (RLS por dueño), `planes_empresa` (lectura pública/escritura admin; soporta planes privados por empresa vía `empresa_id`), `precios_documentos` (lectura pública/escritura admin, **sin precio** — el campo `precio` quedó nullable y sin usar), `configuracion_persona` (fila única, tarjeta "Persona independiente"), `configuracion_portal` (fila única, lectura pública/escritura admin — identidad del portal, redes sociales, WhatsApp, correo de contacto, texto legal del footer), `configuracion_landing` (fila única, textos + interruptores de la página de inicio), `bloques_landing` (multi-fila, bloques libres de texto+imagen+botón al final de `/`), `paginas` (multi-fila, páginas propias con URL propia tipo `/paginas/<slug>`). Bucket de Storage `portal-assets` (público para leer, admin-only para escribir) para logo/favicon/imágenes de bloques. SQL completo de todas en el README → "Base de datos (Postgres)".

⚠️ **Gotcha de JSX con comillas escapadas**: al construir `LandingConfigManager.tsx` un `label="Título \"X\""` rompió el build en Vercel (`Parsing ecmascript source code failed`) — `\"` no es un escape válido dentro de un atributo JSX de comillas dobles (a diferencia de un string JS normal). Usar comillas tipográficas (`“ ”`) en su lugar, como ya se hacía en el resto del archivo.

`configuracion_portal`: el **logo, el favicon y el color primario** ya se leen en vivo (`Header.tsx` hace un `select` client-side de `logo_url`/`color_primario`; `layout.tsx` usa `generateMetadata` + `export const revalidate = 60` para `favicon_url`, así el cambio se ve en máximo 60s sin redeploy). **Nombre del portal y eslogan todavía no** — siguen fijos en el código, pendiente en el roadmap.

⚠️ **Gotcha del color primario**: la paleta de marca vivía dentro de `@theme inline` en `globals.css`, y Tailwind v4 "hornea" el hex literal en cada clase cuando el bloque es `inline` (ej. `.bg-brand-blue{background-color:#1d4ed8}`) — eso hacía imposible cambiar el color en runtime, por más que `/admin` lo guardara bien. Se movió la paleta de marca a un `@theme` normal (sin `inline`), que sí genera `var(--color-brand-blue)`; `Header.tsx` ahora sobreescribe esa variable con `document.documentElement.style.setProperty` al cargar `color_primario`. Si algún color de marca "no cambia" pese a estar bien guardado en la base de datos, revisar que no haya vuelto a caer dentro de un bloque `@theme inline`.

⚠️ **Gotcha del favicon**: Next.js detecta automáticamente un archivo `src/app/icon.png` (convención de archivo) y ese **siempre gana** sobre cualquier `metadata.icons` dinámico puesto en `generateMetadata` — por eso el favicon de `/admin` no se reflejaba (había un `icon.png` estático ahí). Se borró ese archivo; si algún día el favicon vuelve a "no cambiar" pese a estar bien guardado en la base de datos, lo primero que hay que revisar es que no haya reaparecido un `src/app/icon.*`.

Planes de empresa actuales (6, nombres definidos por el usuario): **Lite, Standard, Advanced, Professional, Business, Enterprise** (créditos 10/25/50/100/250/500, "Professional" marcado como destacado, "Enterprise" como cotización — `mostrar_precio_desde: true`, sin precio anual). Los precios y créditos exactos son **valores de ejemplo** que puse yo mismo (el usuario no dio cifras) — no asumir que son los precios reales sin confirmarlo; el usuario los puede editar él mismo desde `/admin` → Planes de empresa.

**Dar/quitar acceso admin ya tiene UI real**: pestaña "Administradores" en `/admin` → `AdminRolesManager.tsx` → `POST /api/admin/roles` (Route Handler que usa la Service Role Key server-side, verifica que quien llama ya sea admin antes de hacer nada). Ya no hace falta la vía manual por SQL, aunque sigue funcionando: `update auth.users set raw_app_meta_data = raw_app_meta_data || jsonb_build_object('role','admin') where email = '...';` (usa `app_metadata`, no `user_metadata`, porque el usuario no puede modificarse ese campo a sí mismo). Cuenta admin actual: `yorbis10@gmail.com`.

⚠️ **Gotcha de Supabase**: `auth.admin.updateUserById(id, { app_metadata })` hace *merge*, no reemplaza — omitir una clave (`delete obj.role`) no la borra, hay que mandar `role: null` explícitamente para quitarla. Ya costó un bug real (revoke no quitaba el acceso) — cuidado si se toca ese endpoint.

⚠️ **Gotcha de esta sesión con `SUPABASE_SERVICE_ROLE_KEY`**: el copiar/pegar (Ctrl+V) entre pestañas del navegador automatizado de Claude Code **no funciona de forma confiable** en esta máquina — el paste se "registra" pero el campo queda vacío, sin error visible. Causó horas de debugging (error "supabaseKey is required" en producción) hasta notar que el textarea de Vercel tenía `length: 0`. Solución que funcionó: leer el valor del campo origen vía JS (`element.value`), y **escribirlo directamente** con la acción de teclado (`type`), no pegarlo. Además, una vez guardada una env var como "Sensitive" en Vercel, no se puede volver a copiar/ver el valor — si algo sale mal hay que borrarla y crearla de nuevo. Por eso `SUPABASE_SERVICE_ROLE_KEY` quedó como variable normal (no Sensitive) en Vercel; sigue siendo server-only y nunca se expone al navegador, "Sensitive" es solo una capa extra de ocultamiento en el dashboard.

**Pasarela de pagos decidida y flujo de personas construido** (2026-08-09): el usuario preguntó qué pasarela recomendaba entre Wompi/PayU/Bold — se comparó por tarifas (Wompi 2,99%+IVA+$600 tarjeta / Bold 3,29%+$300 / PayU 2,9%-3,9% variable) y ganó **Wompi**, tanto por precio como por integración más directa. El usuario dijo "podemos continuar con Wompi aunque aún me hagan falta otras integraciones" — aclaró en el momento que **su cuenta de Wompi sigue en validación y no tiene ninguna llave, ni de sandbox**. Se construyó igual todo el flujo de `/solicitar` (checklist de documentos para personas → `POST /api/solicitudes/crear` → checkout hospedado de Wompi → `POST /api/webhooks/wompi` actualiza el estado → `/solicitar/confirmacion` lo muestra), con un **fallback explícito**: si las llaves de Wompi no están configuradas, la solicitud igual se guarda en la tabla nueva `solicitudes` con `estado="pendiente"` y la UI dice "los pagos estarán disponibles pronto" en vez de romperse. El precio es fijo por solicitud (`configuracion_persona.precio_desde`), confirmado con el usuario — no depende de cuántos documentos se elijan, por eso `precios_documentos.precio` sigue sin usarse. Verificado end-to-end en producción (creé una solicitud de prueba con sesión real, confirmé la fila en Supabase, la borré). **La firma de integridad del checkout y el checksum del webhook se escribieron de memoria siguiendo la documentación pública de Wompi, sin poder probarlos contra una respuesta real** — es lo primero que hay que verificar en cuanto el usuario tenga llaves de sandbox, junto con los nombres exactos de los parámetros del checkout hospedado. Los botones "Solicitar mis documentos" de `/` ahora apuntan a `/solicitar` en vez de `/registro`.

**Módulo de admin para las llaves de Wompi + primera prueba real contra Wompi** (mismo día, a pedido de "agreguemos un módulo en admin para la integración de Wompi: donde se tengan que poner los campos..."): las tres llaves dejaron de ser variables de entorno de Vercel y pasaron a vivir en una tabla nueva `configuracion_wompi`, editable desde `/admin` → **Pagos (Wompi)** (`WompiConfigManager.tsx` + `/api/admin/wompi-config`, mismo patrón `requireAdmin` que `/api/admin/roles`). Decisión de seguridad: la tabla tiene RLS activado **sin ninguna policy** para anon/authenticated (deniega todo por defecto), así que solo la Service Role Key puede leerla — ni siquiera el admin la lee directo desde el navegador. El endpoint `GET` nunca devuelve los dos secretos, solo si están configurados o no; el formulario los muestra como campos de contraseña vacíos ("ya hay uno guardado, déjalo en blanco para no cambiarlo"), mismo patrón "write-only" que ya se usaba para `SUPABASE_SERVICE_ROLE_KEY` en Vercel. `/api/solicitudes/crear` y `/api/webhooks/wompi` se actualizaron para leer de esta tabla en vez de `process.env`. Verificado end-to-end: guardé llaves de prueba inventadas desde el formulario nuevo (banner pasó de ámbar a verde), y **`/solicitar` sí redirigió al checkout real de `checkout.wompi.co`** mostrando "MODO DE PRUEBAS" — Wompi rechazó la llave falsa con el error esperado, pero esto confirma que el formato de los parámetros del checkout (`public-key`, `amount-in-cents`, `reference`, `signature:integrity`, `redirect-url`) es correcto. Lo único que sigue sin validar contra una cuenta real es si la firma de integridad y el checksum del webhook son válidos — eso solo se puede probar con llaves de sandbox reales. Datos de prueba borrados después de verificar.

**Sandbox y Producción separados, con interruptor** (mismo día, a pedido de "en admin debería estar mapeado los 2 ambientes (Sandbox y Producción) y un toggle para intercambiar, ya que no existe una sesión de pruebas de la plataforma Colombia Contrata" + "también incluir los campos de URL base para los 2 ambientes"): se rehizo `configuracion_wompi` para guardar las 4 credenciales (URL base, llave pública, secreto de integridad, secreto de eventos) **por separado para Sandbox y para Producción**, más una columna `ambiente_activo` que decide cuál usan los endpoints. Como el sitio no tiene un despliegue de pruebas aparte, esto deja guardar las llaves de los dos ambientes a la vez sin que se pisen, y cambiar cuál está en vivo con un interruptor (dos botones Sandbox/Producción en `/admin` → Pagos (Wompi), con `confirm()` antes de pasar a Producción porque ahí empieza a cobrar dinero real). Las URLs base vienen precargadas con los valores oficiales (`sandbox.wompi.co/v1` / `production.wompi.co/v1`, confirmados contra la doc que el usuario compartió: docs.wompi.co/docs/colombia/ambientes-y-llaves) aunque el código todavía no las usa (no se llama la API REST de Wompi directamente, solo el checkout hospedado y el webhook) — quedan ahí listas para cuando haga falta. La tabla se recreó desde cero (drop + create) ya que solo tenía datos de prueba; verificado en vivo que las dos secciones renderizan bien con sus URLs base por defecto. No se probó el clic en "Producción" en el navegador automatizado porque el `confirm()` de JS no se puede confirmar en esta máquina (mismo gotcha de siempre) — se verificó por revisión de código en vez de interacción.

**Compra de planes de empresa + panel de admin de usuarios/pagos** (2026-08-16, mismo día): el usuario pidió "llevar el control de usuarios y pagos, y también los planes para empresa (los cuales realizan 1 solo pago al año y pagos mensual)". Antes de construir se preguntó (a) si el cobro mensual debía ser automático/recurrente o manual cada período — eligió **manual** ("como ya funciona para personas"), evitando así tokenizar tarjetas y programar cobros; y (b) qué necesitaba exactamente en el admin — respondió los tres: ver tabla de pagos, ver cuentas de usuario, y poder marcar pagos a mano, más "poder activar o inactivar usuarios". Se construyó: `/empresas/planes` (mismo patrón que `/solicitar` pero para empresas, tabla nueva `pagos_empresa` con `plan_nombre`/`creditos` copiados al momento de la compra para no depender del plan en vivo, y `fecha_vencimiento` calculada según el período al aprobarse el pago); se extrajo `src/lib/wompi.ts` (`getWompiKeys`/`buildWompiCheckoutUrl`) para no duplicar la lógica de firma entre personas y empresas; el webhook ahora distingue `solicitudes` de `pagos_empresa` por el prefijo de la referencia (`SOL-`/`EMP-`). En `/admin` se agregó el grupo "Usuarios y pagos": pestaña **Usuarios** (lista cruzando `auth.admin.listUsers` con `profiles`, activar/desactivar via `ban_duration` de Supabase Auth — no existe un flag "activo" directo) y pestaña **Pagos** (solicitudes + pagos_empresa combinados, filtros, botón "marcar pagado" para pagos que llegaron por fuera de Wompi). Verificado en producción con datos reales: la lista de usuarios mostró las cuentas reales del sitio correctamente (incluidas las insignias "Admin"), y la lista de pagos mostró — y permitió detectar y limpiar — varias solicitudes de prueba que habían quedado de sesiones anteriores. **No se probó la compra completa de un plan de empresa** (solo el *gate* de cuenta) porque no había una cuenta de empresa de prueba a mano en el navegador — pendiente probarlo la próxima vez que se pueda.

Email: **Resend conectado como SMTP de Supabase Auth** — correos salen de `noreply@colombiacontrata.com`. Las 6 plantillas de "Authentication" están traducidas al español. De las 7 de "Security", **"Change Email Address" ya está activada y traducida** (la usa `AccountSecurityForm.tsx` al cambiar de correo); el resto (MFA, cambios de password/phone) siguen desactivadas y sin traducir porque no se usan. En Supabase → Authentication → URL Configuration, el **Site URL** y los **Redirect URLs** están en `colombiacontrata.com`/`www.colombiacontrata.com` (además de `colombia-contrata.vercel.app/**`) — importante si se agrega otro dominio desde el que la gente pueda registrarse, porque si el `redirect_to` dinámico (`window.location.origin` en `RegisterForm.tsx`) no está en esa lista, Supabase lo ignora y usa el Site URL de respaldo (ya pasó una vez con el dominio de Vercel).

Dominio: `colombiacontrata.com` **ya conectado a Vercel** (2026-08-07). En Vercel → Domains están `colombiacontrata.com` + `www.colombiacontrata.com` (redirect apex→www, 308), ambos "Valid Configuration". En Cloudflare DNS existen los dos registros CNAME (`@` y `www`) apuntando a `1b7885c77d62091c.vercel-dns-017.com.`, con el proxy de Cloudflare desactivado (DNS only / nube gris) en ambos.

Identidad de marca ya aplicada: logo/ícono reales (`public/isotipo.png`, `public/icono.png`), paleta de colores (`brand-navy #0d1b3d`, `brand-blue #1d4ed8`, `brand-yellow #fcd116`, `brand-red #ce1126` como tokens Tailwind en `globals.css`), tipografía Montserrat. El azul es una decisión explícita del usuario: el Manual de Identidad Visual original proponía `#0033a0` (azul bandera de Colombia), pero el usuario pidió cambiarlo a `#1d4ed8` — no revertir sin confirmar con él primero.

## Cumplimiento legal (Habeas Data — Ley 1581 de 2012)
- Antecedentes penales/policiales/disciplinarios son **dato sensible** → requieren checkbox de autorización explícito y separado del checkbox de Términos y Condiciones. Nunca premarcado. Ya implementado en `/registro` (3 checkboxes independientes).
- La trazabilidad del consentimiento **ya se guarda** en `user_metadata` de Supabase al hacer `signUp` (booleans de cada checkbox + `policy_version` + `accepted_at`, ver `POLICY_VERSION` en `RegisterForm.tsx`). Falta la **IP** — `signUp` corre client-side y no tiene acceso a ella, necesitaría un Route Handler.
- Evaluar registro en el RNBD (Registro Nacional de Bases de Datos) ante la SIC si se supera el umbral de registros.
- `/terminos` y `/privacidad` son plantillas base con aviso visible de que faltan revisión legal y datos reales de la empresa (razón social, NIT) — no están listas para producción tal cual.

## Stack
- **Frontend**: Next.js 16 (TypeScript, App Router, Tailwind CSS v4), Montserrat como tipografía
- **UI**: Preline UI (elegido por el usuario por su variedad de componentes)
- **Pagos (Colombia)**: **Wompi**, decidido el 2026-08-09 (comparado con PayU y Bold por tarifas — ver detalle abajo). Flujo de personas (`/solicitar`) ya construido y desplegado, pero sin llaves reales todavía (cuenta del usuario en validación) — ver README → "Solicitud de documentos y pago con Wompi"
- **Auth + DB + Storage**: **Supabase confirmado y conectado** (proyecto `colombia-contrata`, ref `zjbijmieiyumpqwyqhfm`, región São Paulo). Falta storage con URLs firmadas para la expiración de 10 días de los documentos de personas (el bucket `portal-assets` que ya existe es solo para logo/favicon del admin, no para eso).
- **Email**: **Resend confirmado y conectado** como SMTP de Supabase Auth (`smtp.resend.com`, remitente `noreply@colombiacontrata.com`). Plantillas de "Authentication" traducidas al español.
- **API de consultas a fuentes**: aún no la tiene el usuario — se está construyendo el sitio en paralelo mientras la consigue

## Infraestructura y despliegue
- Dominio comprado: `colombiacontrata.com`, DNS/CDN en Cloudflare, **ya conectado a Vercel** (ver detalle en README → Despliegue).
- Repo remoto: https://github.com/servisolucionesaragon/Colombia_Contrata (público, main branch). Credenciales de git ya configuradas en esta máquina (Git Credential Manager, usuario `servisolucionesaragon`) — el push funciona sin pedir login.
- Hosting: Vercel (proyecto `servisoluciones-aragon/colombia-contrata`), tier gratuito, autodeploy en cada push a `main`.
- ⚠️ **Nunca marcar las env vars `NEXT_PUBLIC_*` como "Sensitive" en Vercel** — esa opción las oculta durante el build, y como necesitan insertarse en el bundle del navegador en build-time, el build falla con `supabaseKey is required`. Ya pasó una vez (commits alrededor de `4658785`). La anon key de Supabase está diseñada para ser pública.
- La sesión del navegador (Vercel/Supabase/Cloudflare) que uso para tareas de panel **no persiste entre sesiones de Claude Code** — si hace falta re-loguearse, hay que pedírselo al usuario (nunca ingresar sus credenciales).

## Entorno de desarrollo local — importante
El proyecto vive en una unidad de red mapeada (`Z:\`, SMB sobre lo que parece ser una VPN Tailscale). Esto causa:
- Instalaciones de npm lentas y a veces fallidas a mitad de camino (`ENOTEMPTY`/`EPERM`) — solución: borrar `node_modules` (usar PowerShell `Remove-Item -Recurse -Force` si `rm -rf` de Git Bash falla) y reinstalar.
- Turbopack (default de `next dev` y `next build` en Next 16) falla con las rutas UNC de la unidad de red — el script `dev` fuerza `--webpack`, y `next.config.ts` declara `turbopack: {}` para que `next build` (usado por Vercel) no falle por tener un `webpack()` custom sin config de turbopack.
- El watcher de archivos nativo falla en la unidad de red y hace que Next.js reinicie el servidor en bucle — mitigado con `WATCHPACK_POLLING=true`/`CHOKIDAR_USEPOLLING=true` (vía `cross-env` en el script `dev`) y `watchOptions.poll` en `next.config.ts`.
- Aun con todo esto, la primera compilación local puede tardar **hasta 18 minutos**. El servidor local a veces sigue cayéndose solo.

**Por esto, el flujo de trabajo establecido en esta sesión es: editar → commit → push a `main` → verificar en el deploy de Vercel** (tarda ~15-30s en compilar y no tiene ninguno de estos problemas), en vez de depender de `npm run dev` local para verificar cambios. Ver detalle completo en el README.

## Correo corporativo (buzones)
El usuario quiere buzones completos (no solo redirección) para direcciones como `contacto@colombiacontrata.com` — se le recomendó **Google Workspace** (~$7 USD/usuario/mes) por ser lo más confiable y familiar (Gmail). Pendiente que el usuario cree la cuenta de Workspace (implica pago, no lo hago yo); cuando la tenga, Google le va a pedir agregar un TXT de verificación y cambiar los registros MX del dominio en Cloudflare — hay que revisar que el cambio de MX no rompa el envío de correos transaccionales de Supabase/Resend (que usa Resend como SMTP, no depende del MX del dominio para *enviar*, pero conviene verificarlo igual al hacer el cambio).

## Preferencias de colaboración
- El usuario no tiene conocimiento técnico profundo (ej. pidió explicación simple de qué es Vercel) — explicar conceptos de forma accesible, sin asumir jerga previa.
- Prefiere avanzar construyendo el sitio mientras resuelve piezas externas (como el API del proveedor de documentos) en paralelo.
- El usuario ya decidió comprometerse con backend real para los flujos principales (registro, login, perfil, admin) — ya no hace falta preguntar "interfaz primero vs. backend real" para estos. Sigue siendo válido preguntar para features nuevos que toquen infraestructura externa (pagos, API del proveedor de documentos, storage).
- El usuario tiene assets de marca reales (logo, ícono, manual de identidad visual) en `Z:\SSA\Proyectos SSA\Colombia Contrata\Imagenes` (fuera de este repo) — revisar esa carpeta antes de asumir placeholders quedan definitivos.
